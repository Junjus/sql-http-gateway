services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sqltojson
      POSTGRES_USER: sqltojson
      POSTGRES_PASSWORD: sqltojson
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sqltojson -d sqltojson"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
    volumes:
      - ./queries:/app/queries:ro
    depends_on:
      - db
    command:
      - -addr
      - :8080
      - -sql-dir
      - /app/queries
      - -driver
      - pgx
      - -dsn
      - postgres://sqltojson:sqltojson@db:5432/sqltojson?sslmode=disable
    ports:
      - "8080:8080"

volumes:
  pgdata:
